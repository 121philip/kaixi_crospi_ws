#!/bin/bash
#
# interactive etasl/lua shell
#


#
# get directory where script is located in the ${DIR} variable
# (in a robust way)
#
SOURCE="${BASH_SOURCE[0]}"
#while [ -h "$SOURCE" ]; do # resolve $SOURCE until the file is no longer a symlink
#  DIR="$( cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd )"
#  SOURCE="$(readlink "$SOURCE")"
#  [[ $SOURCE != /* ]] && SOURCE="$DIR/$SOURCE" # if $SOURCE was a relative symlink, we need to resolve it relative to the path where the symlink file was located
#done
DIR="$( cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd )"



#
# add appropriate LUA_PATH and LUA_CPATH for etasl:
#

if [ "x$LUA_PATH" = "x" ]; then
    LUA_PATH=";"
fi
export LUA_PATH="$LUA_PATH;/home/masterthesis/kaixi_crospi_ws/install/etasl_wrapper/lib/etasl1.5/?.lua"
 
if [ "x$LUA_CPATH" = "x" ]; then
    LUA_CPATH=";"
fi
export LUA_CPATH="$LUA_CPATH;/home/masterthesis/kaixi_crospi_ws/install/etasl_wrapper/lib/?.so"

pathadd() {
    newelement=${1%/}
    if [ -d "$1" ] && ! echo $PATH | grep -E -q "(^|:)$newelement($|:)" ; then
        if [ "$2" = "after" ] ; then
            PATH="$PATH:$newelement"
        else
            PATH="$newelement:$PATH"
        fi
    fi
}

pathrm() {
    PATH="$(echo $PATH | sed -e "s;\(^\|:\)${1%/}\(:\|\$\);\1\2;g" -e 's;^:\|:$;;g' -e 's;::;:;g')"
}


${DIR}/ilua.lua "$@"


